using System.Data.SqlClient;

namespace Project1
{
    class Program
    {
        static void Main(string[] args)
        {
            SqlConnection conn = new SqlConnection();
            conn.ConnectionString = "Data Source =  DESKTOP-PFCVLHM; Initial Catalog = ProgrammingProject; Integrated Security= true";

            

            String[] Universityname = {"Technological University","Medical University","University of Froeign Language", 
                                           "BA,BSc","University of Economic"};
            String[] TechnologicalUniversity ={"Architecture","Cival","Mechanical Power","Mechatronic","Information Technology","Electrical Power",
                                              "Electric Communication","Biochemical","Mining","Nuclear"};
            String[] MedicalUniversity = { "Medical", "Dental", "Nursing", "Vet" };
            String[] ForeignLanguage = { "English", "Janpanese", "Korea", "Thailand", "Chinese", "French", "German", "Russian" };
            String[] Economic = { "B.A Eco (Bachelor of Art Economic)", "B.A(Stastics)", "B.com(Bachelor of Commerce)" };
            String[] BAorBSc = { "Myanmar", "English", "Maths", "Physics", "Chemistry", "Zoology", "Botany", "Psychology", 
                                   "Internations Relations", "Law" };
            Console.WriteLine("Enter your marks");
            String mark = (Console.ReadLine());
            int iMark = int.Parse(mark);
            if (iMark>=495)
            {
                for (int i = 0; i < Universityname.Length; i++)
                {
                    Console.WriteLine("You can join to " + Universityname[i]);
                }
                Console.WriteLine("Choose an option for your choice of University");
                for (int i = 0; i < Universityname.Length; i++)
                {
                    Console.WriteLine(i+1 + " --> "+Universityname[i]);
                }


                String data=(Console.ReadLine());
                int option = int.Parse(data);
                if (option == 1 )
                {
                    for (int i = 0; i < TechnologicalUniversity.Length;i++ )
                    {
                        Console.WriteLine("You can join to " + TechnologicalUniversity[i]);
                    }
                }
                if (option == 2)
                {
                    for (int i = 0; i < MedicalUniversity.Length; i++)
                    {
                        Console.WriteLine("You can join to " + MedicalUniversity[i]);
                    }
                }
                if (option == 3)
                {
                    for (int i = 0; i < ForeignLanguage.Length; i++)
                    {
                        Console.WriteLine("You can join to " + ForeignLanguage[i]);
                    }
                }
                if (option == 4)
                {
                    for (int i = 0; i < Economic.Length; i++)
                    { 
                        Console.WriteLine("You can join to " +Economic[i]);
                    }
                }
                if (option == 5)
                {
                    for (int i = 0; i < BAorBSc.Length; i++)
                    {
                        Console.WriteLine("You can join to " + BAorBSc[i]);
                    }
                }
                Console.WriteLine("Choose the Major You Want to Study");
                    String major=(Console.ReadLine());
                    Console.WriteLine("Thank for Your Choice " + major);
            }
            else if (iMark >= 492)
            {
                for (int i = 1; i < 5; i++)
                {
                    Console.WriteLine("You can join to " + Universityname[i]);
                }
                Console.WriteLine("Choose an option for your choice of University");
                for (int i = 1; i < Universityname.Length; i++)
                {
                    Console.WriteLine(i+ 1 + " --> " + Universityname[i]);
                }
                String data = (Console.ReadLine());
                int option = int.Parse(data);
                if (option == 2)
                {
                    for (int i = 0; i < MedicalUniversity.Length; i++)
                    {
                        Console.WriteLine("You can join to " + MedicalUniversity[i]);
                    }
                }
                if (option == 3)
                {
                    for (int i = 0; i < ForeignLanguage.Length; i++)
                    {
                        Console.WriteLine("You can join to " + ForeignLanguage[i]);
                    }
                }
                if (option == 4)
                {
                    for (int i = 0; i < Economic.Length; i++)
                    {
                        Console.WriteLine("You can join to " + Economic[i]);
                    }
                }
                if (option == 5)
                {
                    for (int i = 0; i < BAorBSc.Length; i++)
                    {
                        Console.WriteLine("You can join to " + BAorBSc[i]);
                    }
                }
            }
            else if (iMark >= 470)
            {
                for (int i = 2; i < 5; i++)
                {
                    Console.WriteLine("You can join to " + Universityname[i]);
                }
                Console.WriteLine("Choose an option for your choice of University");
                for (int i = 2; i < Universityname.Length; i++)
                {
                    Console.WriteLine(i + 1 + " --> " + Universityname[i]);
                }
                String data = (Console.ReadLine());
                int option = int.Parse(data);
                
                if (option == 3)
                {
                    for (int i = 0; i < ForeignLanguage.Length; i++)
                    {
                        Console.WriteLine("You can join to " + ForeignLanguage[i]);
                    }
                }
                if (option == 4)
                {
                    for (int i = 0; i < Economic.Length; i++)
                    {
                        Console.WriteLine("You can join to " + Economic[i]);
                    }
                }
                if (option == 5)
                {
                    for (int i = 0; i < BAorBSc.Length; i++)
                    {
                        Console.WriteLine("You can join to " + BAorBSc[i]);
                    }
                }
            }
            else if (iMark >= 440)
            {
                for (int i = 3; i < 5; i++)
                {
                    Console.WriteLine("You can join to " + Universityname[i]);
                }
                Console.WriteLine("Choose an option for your choice of University");
                for (int i = 3; i < Universityname.Length; i++)
                {
                    Console.WriteLine(i + 1 + " --> " + Universityname[i]);
                }
                String data = (Console.ReadLine());
                int option = int.Parse(data);

                if (option == 4)
                {
                    for (int i = 0; i < Economic.Length; i++)
                    {
                        Console.WriteLine("You can join to " + Economic[i]);
                    }
                }
                if (option == 5)
                {
                    for (int i = 0; i < BAorBSc.Length; i++)
                    {
                        Console.WriteLine("You can join to " + BAorBSc[i]);
                    }
                }
            }
            else if (iMark >= 400)
            {
                for (int i = 5; i < 5; i++)
                {
                    Console.WriteLine("You can join to " + Universityname[i]);
                }
                Console.WriteLine("Choose an option for your choice of University");
                for (int i = 4; i < Universityname.Length; i++)
                {
                    Console.WriteLine(i + 1 + " --> " + Universityname[i]);
                }
                String data = (Console.ReadLine());
                int option = int.Parse(data);

                
                if (option == 5)
                {
                    for (int i = 0; i < BAorBSc.Length; i++)
                    {
                        Console.WriteLine("You can join to " + BAorBSc[i]);
                    }
                }
            }
        }
    }
}